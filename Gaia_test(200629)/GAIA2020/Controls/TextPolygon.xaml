<UserControl x:Class="GAIA2020.Controls.TextPolygon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GAIA2020.Converters"
             mc:Ignorable="d"
             >
    <Viewbox DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl, Mode=FindAncestor}}">
        <Viewbox.Resources>
            <local:BooleanToColorConverter x:Key="BooleanToColorConverter" />
            <local:BooleanToSizeConverter x:Key="BooleanToSizeConverter" />
        </Viewbox.Resources>
        <Grid>            
            <Path Loaded="Path_Loaded" x:Name="path" Data="{Binding PathData}" Stretch="UniformToFill" Fill="{Binding PathFill}" Stroke="{Binding IsSelected, Converter={StaticResource BooleanToColorConverter}}" 
                  StrokeThickness="{Binding IsSelected, Converter={StaticResource BooleanToSizeConverter}}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock x:Name="textBlock" Text="{Binding PolygonText}" FontFamily="Gulim" IsHitTestVisible="False"/>
        </Grid>
    </Viewbox>
</UserControl>