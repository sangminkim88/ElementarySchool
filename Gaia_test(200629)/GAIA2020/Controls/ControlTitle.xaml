<UserControl x:Class="GAIA2020.Controls.ControlTitle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" VerticalAlignment="Top"
             >

    <StackPanel Height="16" Grid.ColumnSpan="4" Orientation="Horizontal" VerticalAlignment="Top" 
                DataContext="{Binding RelativeSource={RelativeSource AncestorType=UserControl, Mode=FindAncestor}}"
                >
        <Label Padding="0" Style="{StaticResource roundTitleLabel}" MinWidth="16" MinHeight="16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text=" "/>
                <TextBlock Text="{Binding Code}"/>
                <TextBlock Text=" "/>
            </StackPanel>
        </Label>
        <TextBlock Text="{Binding Title}" Padding="0" VerticalAlignment="Center" Foreground="{StaticResource Theme.Color0}" FontSize="13" Margin="10,0,0,0"/>
    </StackPanel>
</UserControl>
