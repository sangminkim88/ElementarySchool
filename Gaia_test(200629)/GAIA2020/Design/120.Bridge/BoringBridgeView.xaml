<frwk:AUserControl x:Class="GAIA2020.Design.BoringBridgeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GAIA2020.Design"
             xmlns:frwk="clr-namespace:HMFrameWork.Ancestor;assembly=HMFrameWork"
             mc:Ignorable="d" 
             d:DesignHeight="900" d:DesignWidth="1080">
    <frwk:AUserControl.DataContext>
        <local:BoringBridgeViewModel/>
    </frwk:AUserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="860"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Vertical">
            <local:BoringView Grid.Column="0"/>
            <Button Command="{Binding CommandViewChangeTest}" CommandParameter="0" Content="Test1"/>
            <Button Command="{Binding CommandViewChangeTest}" CommandParameter="1" Content="Test2"/>
        </StackPanel>
        <ContentControl Content="{Binding CurrentVM}" Grid.Column="1"/>
    </Grid>
</frwk:AUserControl>
